import { GameContainer } from "./gameContainer";

export class Sounds {
    //beep: Sound;

    constructor(container: GameContainer) {
        //this.beep = new Sound("beep", "/assets/base/sounds/beep.wav", container.scene);
    }
}

